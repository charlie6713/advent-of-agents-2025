## day 22

Today I learned that prompt engineering alone cannot guarantee security; robust LLM systems require defense-in-depth with deterministic controls like preprocessing callbacks, gateway model filtering, and strict agent identity and authorization.