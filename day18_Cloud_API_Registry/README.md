## day 18

I learned that Vertex AI Agent Builder can use Cloud API Registry as a centralized tool repository, which allows admins to control approved tools while developers fetch them dynamically without hardcoding APIs.